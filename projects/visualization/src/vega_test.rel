def mydata = {(:x, 1, 0.0); (:x, 2, 0.5); (:x, 3, 1.0);
              (:y1, 1, 1.0); (:y1, 2, 2.0); (:y1, 3, 0.5);
              (:y2, 1, 2.0); (:y2, 2, 4.0); (:y2, 3, 0.0);
              (:y3, 1, 3.0); (:y3, 2, 1.0); (:y3, 3, 2.0);}

def yfield = {(1,:y1);(2,:y2);}
def cfield = {(:y1,2);(:y2,3);}


def my_line_chart = line_chart
def my_line_chart = create_chart[:data,:multiy,:color,mydata,:x,yfield,cfield]